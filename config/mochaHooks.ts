import { chromium,Browser,Page } from "playwright";
let browser:Browser,page:Page;
export class confHooks{
    // // @ts-ignore
    // before(async():void => {
    //     page = await browser.newPage();
    // });
}